meta {
  name: set concert
  type: http
  seq: 8
}

patch {
  url: http://localhost:3000/api/concerts/1
  body: json
  auth: inherit
}

body:json {
  {
    "title": "FMLBBBBBB",
    "location": 9
  }
}

assert {
  res.status: eq 200
  res.body: isArray
}

settings {
  encodeUrl: true
  timeout: 0
}
