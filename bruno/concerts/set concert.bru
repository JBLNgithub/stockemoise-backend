meta {
  name: set concert
  type: http
  seq: 8
}

patch {
  url: http://localhost:3000/api/concerts/17
  body: json
  auth: inherit
}

body:json {
  {
    "title": "fau ??"
  }
}

assert {
  res.status: eq 200
  res.body: isArray
}

settings {
  encodeUrl: true
  timeout: 0
}
