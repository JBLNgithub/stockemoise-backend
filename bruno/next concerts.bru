meta {
  name: next concerts
  type: http
  seq: 2
}

get {
  url: http://localhost:3000/api/concerts/next?limit=3
  body: none
  auth: inherit
}

params:query {
  limit: 3
}

assert {
  res.status: eq 200
  res.body: isArray
}

settings {
  encodeUrl: true
}
