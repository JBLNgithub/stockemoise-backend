meta {
  name: set concert
  type: http
  seq: 8
}

patch {
  url: http://localhost:3000/api/concerts
  body: json
  auth: inherit
}

body:json {
  {
    "title": "test concert (A SUPPRIMER)"
  }
}

assert {
  res.status: eq 200
  res.body: isArray
}

settings {
  encodeUrl: true
  timeout: 0
}
