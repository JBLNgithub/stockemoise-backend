meta {
  name: concert
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/api/concerts/3
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
